# Copy this file to frontend/.env for local development.
# When frontend + backend are deployed together on one domain (see render.yaml),
# this can be left unset — '/api' automatically points at the same domain.

VITE_API_BASE_URL=http://localhost:8000/api
